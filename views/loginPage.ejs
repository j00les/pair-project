<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      .form {
        border: solid;
        width: 500px;
        margin: 30px auto;
      }
      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 20px;
        justify-content: space-between;
      }
      label {
        display: flex;
        flex-direction: column;
      }
      /* navbar */
      body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
      }

      .topnav {
        overflow: hidden;
        background-color: #333;
      }

      .topnav a {
        float: left;
        color: #f2f2f2;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
        font-size: 17px;
      }

      .topnav a:hover {
        background-color: #ddd;
        color: black;
      }
    </style>
    <title>Login Page</title>
  </head>
  <body>
    <nav class="topnav">
      <a href="/register/">Register</a>
    </nav>
    <section class="form">
      <% if (error) { %>
      <p><%= error %></p>
      <% } %>
      <form action="/login" method="post">
        <h1>Login Page</h1>
        <label for="email">
          Username:
          <input id="email" name="username" type="text" />
        </label>
        <label for="email">
          Password:
          <input type="password" id="email" name="password" type="text" />
        </label>
        <button type="submit">Login</button>
      </form>
    </section>
  </body>
</html>
